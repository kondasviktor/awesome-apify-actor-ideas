name: Sync to Hugging Face

on:
  push:
    branches: [main]
    paths:
      - "hf-space/**"
      - ".github/workflows/sync-huggingface.yml"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5

      - name: Upload Static Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_REPO_ID: kondasviktor/awesome-apify-actor-ideas
        run: |
          set -euo pipefail
          if [[ -z "${HF_TOKEN}" ]]; then
            echo "::error::HF_TOKEN repository secret is missing"
            exit 1
          fi
          uvx hf auth whoami
          uvx hf repos create "${HF_REPO_ID}" --type space --space-sdk static --exist-ok
          uvx hf upload "${HF_REPO_ID}" hf-space \
            --type space \
            --exclude=".git*" \
            --delete="*" \
            --commit-message "Sync from GitHub"
